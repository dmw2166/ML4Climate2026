.DS_Store
.ipynb_checkpoints/
_build/
.idea/
# Syllabus kept local, not published to the site
Syllabus*.pdf
# Wildfire dataset (139 MB, from Zenodo 10.5281/zenodo.11510677) — too big for
# GitHub's 100 MB limit. Kept local; randomforests.ipynb ships saved outputs.
lectures_ML/SupervisedML/FPA_FOD_west_cleaned.csv
# SP2 tutorial: the 900 MB Zenodo arrays and the model weights it caches after
# training. Both are regenerated by running generative_tutorial.ipynb.
Lab_X_test.npy
Lab_Y_test.npy
weights/
# Generator script + cached preprocessed arrays for generative_tutorial.ipynb
.tutorial_build/
lectures_ML/SupervisedML/rnd_multiclass_clf.pkl
lectures_ML/SupervisedML/decision_tree.dot
# Lecture slides, recordings, and Zoom transcripts kept local, not published to the site
*.pptx
*.mp4
*Transcript.md
# Figures/assets extracted from slide decks for reference while writing note pages —
# not published to the site (note pages regenerate figures from data instead)
images_ann/
**/_contact_sheet.html
lectures_ML/DeepLearning/figures/ANNLecture/# Solution keys for the assignments. This repository is PUBLIC and serves the
# course website, so these are kept out of both git and the book build. See
# also exclude_patterns in _config.yml. Do not remove either exclusion.
solutions/

# ClimateBench data for the emulation tutorial (~900 MB, fetched from Zenodo
# 10.5281/zenodo.7064308 by the notebook's first cell). Too big for git.
climatebench_data/
